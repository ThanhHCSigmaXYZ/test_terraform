config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "dim_worker",
  description: "最新の作業者マスタ情報（ディメンションテーブル）",
  tags: ["access", "dimension"],
  bigquery: {
    clusterBy: ["worker_id"]
  }
}

SELECT
  *
FROM
  ${ref("tmp_erp_worker_master_scd2")}
WHERE
  is_current = true
