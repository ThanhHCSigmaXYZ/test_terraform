config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "dim_worker",
  bigquery: {
    clusterBy: ["worker_id"]
  }
}

SELECT
  worker_id,
  name,
  valid_from,
  valid_to
FROM
  ${ref("tmp_erp_worker_master_scd2")}
WHERE
  is_current
