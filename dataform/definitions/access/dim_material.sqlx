config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "dim_material",
  tags: ["access", "dimension"]
}

SELECT
  material_id,
  material_name
FROM
  ${ref("tmp_erp_mat_master_scd2")}
WHERE
  is_current