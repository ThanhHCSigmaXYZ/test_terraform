config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "dim_material",
  bigquery: {
    clusterBy: ["material_id"]
  }
}

SELECT
  material_id,
  description,
  valid_from,
  valid_to
FROM
  ${ref("tmp_erp_mat_master_scd2")}
WHERE
  is_current
