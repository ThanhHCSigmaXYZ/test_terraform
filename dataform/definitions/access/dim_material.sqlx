config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "dim_material"
}

-- This dimension table provides the current version of each material.
SELECT
  material_id,
  description
  -- other attributes...
FROM
  ${ref("tmp_erp_mat_master_scd2")}
WHERE
  is_current
