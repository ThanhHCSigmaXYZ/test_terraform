config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "dim_material",
  description: "品目ディメンションテーブル",
  bigquery: {
    clusterBy: ["material_id"]
  }
}

SELECT
  material_id,
  material_name,
  base_unit
FROM
  ${ref("tmp_erp_mat_master_scd2")}
WHERE
  is_current = TRUE
