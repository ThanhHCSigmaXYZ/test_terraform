config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "dim_material",
  description: "最新の品目マスタ情報（ディメンションテーブル）",
  tags: ["access", "dimension"],
  bigquery: {
    clusterBy: ["material_id"]
  }
}

SELECT
  *
FROM
  ${ref("tmp_erp_mat_master_scd2")}
WHERE
  is_current = true
