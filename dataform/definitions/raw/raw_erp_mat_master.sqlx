config {
  type: "table",
  schema: "dev_dataform_dataset",
  name: "raw_erp_mat_master",
  description: "品目マスタの取り込み",
  bigquery: {
    partitionBy: "etl_loaded_date"
  }
}

SELECT
  *,
  TIMESTAMP(CURRENT_DATE()) AS etl_loaded_at,
  CURRENT_DATE() AS etl_loaded_date
FROM
  ${ref("erp_material_master")}
